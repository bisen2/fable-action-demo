{
  "private": true,
  "scripts": {
    "build": "dotnet fable src && ncc build ./src/Action.fs.js --license licenses.txt"
  },
  "version": "0.0.0",
  "dependencies": {
    "@actions/core": "^1.9.1",
    "@actions/github": "^5.0.3"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.34.0"
  }
}
